{
  "name": "sprocket_docs",
  "version": "0.1.3",
  "license": "MIT",
  "source": "client/src/app.ts",
  "targets": {
    "main": false,
    "docs": {
      "distDir": "priv/static",
      "sourceMap": false
    }
  },
  "devDependencies": {
    "@parcel/packager-ts": "2.9.2",
    "@parcel/transformer-typescript-types": "2.9.2",
    "@tailwindcss/typography": "^0.5.9",
    "@types/jest": "^29.5.2",
    "@types/node": "^20.4.1",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^8.0.1",
    "jest": "^29.5.0",
    "parcel": "^2.9.2",
    "process": "^0.11.10",
    "tailwindcss": "^3.3.1",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "docs:build": "parcel build client/src/app.ts --target docs",
    "docs:watch": "parcel client/src/app.ts client/src/standalone_counter.ts --no-hmr --dist-dir priv/static",
    "standalone_counter:build": "parcel build client/src/standalone_counter.ts --target standalone_counter",
    "tailwind:build": "npx tailwindcss -i ./app.css -o priv/static/app.css",
    "tailwind:watch": "npx tailwindcss -i ./app.css -o priv/static/app.css --watch",
    "clean": "rm -rf build .parcel-cache priv/static/**",
    "watch": "gleam build && concurrently --kill-others \"gleam run -m docs\" \"yarn run docs:watch\" \"yarn run tailwind:watch\"",
    "build": "gleam build && yarn run docs:build && yarn run tailwind:build"
  },
  "dependencies": {
    "sprocket-js": "^0.1.6"
  }
}